"""Domain events for the music bounded context.

NOTE: Music domain events are defined in ``domain.shared.events`` where the
EventBus lives.  This module previously contained a parallel (unused) event
hierarchy.  It was removed to eliminate duplication — see ``shared/events.py``
for the canonical event definitions.
"""
